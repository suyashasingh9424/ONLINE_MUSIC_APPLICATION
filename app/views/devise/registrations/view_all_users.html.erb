<!DOCTYPE html>
<html lang="en">
<head>
  <title>View All Users</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css">
  <style>
    body {
      background-color: #ffdab9;
      color: #333;
    }
    .container {
      background-color: #343a40;
      color: black;
      border-radius: 8px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
      padding: 20px;
      margin-top: 50px;
    }
    .user-card {
      border: 1px solid #ccc;
      border-radius: 8px;
      padding: 20px;
      margin-bottom: 20px;
    }
    th, td {
      vertical-align: middle;
    }
  </style>
</head>
<body>
  <div class="container mt-5">
    <div class="row">
      <div class="col-md-10">
        <h1 class="mb-4 text-center">All Users</h1>
        <div class="user-card">
          <table class="table table-bordered">
            <thead>
              <tr>
                <th style="width: 20%;">Email</th>
                <th style="width: 20%;">Name</th>
                <th style="width: 20%;">Address</th>
                <th style="width: 10%;">Age</th>
                <th style="width: 10%;">Contact</th>
                <th style="width: 10%;">Gender</th>
                <th style="width: 10%;">City</th>
                <th style="width: 10%;">Pincode</th>
                <th style="width: 20%;">Profile Created</th>
                <th style="width: 20%;">Action</th>
              </tr>
            </thead>
            <tbody>
              <% @users.each do |user| %>
                <tr>
                  <td><%= user.email %></td>
                  <td><%= user.name %></td>
                  <td><%= user.address %></td>
                  <td><%= user.age %></td>
                  <td><%= user.contact %></td>
                  <td><%= user.gender %></td>
                  <td><%= user.city %></td>
                  <td><%= user.pincode %></td>
                  <td><%= user.present? ? "Yes" : "No" %></td>
                  <td>
                    <% if user.present? %>
                      <%= link_to "View Profile", show_user_profile_path(id: current_user.id), class: "btn btn-secondary btn-block" %>
                    <% else %>
                      <%= link_to "Create Profile", edit_user_registration_path, class: "btn btn-primary btn-block" %>
                    <% end %>
                  </td>
                </tr>
              <% end %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
</body>
</html>