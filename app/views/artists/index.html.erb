<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>All Artists</title>
  <!-- Bootstrap CSS -->
  <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
  <style>
    /* Custom CSS for additional styling */
    body {
      background-color: #f8f9fa;
      padding-top: 20px;
    }
    .artist-card {
      margin-bottom: 20px;
      border-radius: 10px;
      box-shadow: 0px 2px 6px rgba(0, 0, 0, 0.1);
    }
    .card-header {
      background-color: #007bff;
      color: #fff;
      border-radius: 10px 10px 0 0;
    }
    .btn-primary {
      background-color: #ffc107;
      border-color: #ffc107;
    }
    .btn-primary:hover {
      background-color: #ff9800;
      border-color: #ff9800;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1 class="mb-4">All Artists</h1>
    <div class="row">
      <% @artists.each do |artist| %>
        <div class="col-md-6">
          <div class="card artist-card">
            <div class="card-header">
              <h5 class="card-title m-0"><strong><%= artist.name %></strong></h5>
            </div>
            <div class="card-body">
              <p class="card-text"><%= artist.bio %></p>
              <%= link_to 'Show', artist_path(artist), class: 'btn btn-primary' %>
            </div>
          </div>
        </div>
      <% end %>
    </div>
    <%= link_to 'Back', dashboard_path(current_user.id), class: 'btn btn-secondary mt-3' %>
  </div>
  <!-- Bootstrap JS (optional) -->
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
